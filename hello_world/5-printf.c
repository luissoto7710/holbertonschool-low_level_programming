#include <stdio.h>

/**
 * main - prints the text inside closed captions
 *
 * Return: it returns (0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
